# {{.Name}}

{{.Description}}

## Install

```sh
npm install -g {{.Name}}
```

## Usage

```sh
{{.Name}} --help
{{.Name}} --version
```

### Output formats

```sh
{{.Name}} --format json
{{.Name}} --format yaml
```

## Getting started

Toolchain is pinned in `mise.toml`. Install everything in one go:

```sh
mise install                # installs Node + pnpm (and other pinned tools)
mise run install            # installs ecosystem deps (pnpm install)
cp .env.example .env        # adapter defaults (telemetry, storage, queue, log, config)
```

Optional — start telemetry services (otel-collector + jaeger):

```sh
docker compose -f .devcontainer/docker-compose.yml up -d
open http://localhost:16686 # Jaeger UI for traces
```

## Tool versions

Versions for Node, pnpm, linters, and release tooling are pinned in
`mise.toml` at the project root inside a kit-managed block. Refresh
it with:

```sh
kit init --update
```

### Bring this project up to date

When new defaults land upstream (bumped Node version, new tool):

```sh
kit init --check    # preview drift
kit init --update   # refresh managed blocks
```

See poly-kit's `templates/RUNBOOK-UPGRADE.md` for the full
procedure.

## Development

Prerequisites: `mise` (installs Node.js 22+ and pnpm),
[Task](https://taskfile.dev).

```sh
task setup    # install deps
task check    # lint + test
task build    # compile TypeScript
```

## License

See [LICENSE](LICENSE).

---
Maintained by {{.Author}}.
