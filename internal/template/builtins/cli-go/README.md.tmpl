# {{.Name}}

{{.Description}}

<p align="center">
    <a href="https://github.com/{{.Org}}/{{.Name}}/releases"><img src="https://img.shields.io/github/release/{{.Org}}/{{.Name}}.svg" alt="Release"/></a>
    <a href="https://pkg.go.dev/{{.Module}}?tab=doc"><img src="https://pkg.go.dev/badge/{{.Module}}.svg" alt="GoDoc"/></a>
    <a href="https://github.com/{{.Org}}/{{.Name}}/actions"><img src="https://github.com/{{.Org}}/{{.Name}}/actions/workflows/ci.yml/badge.svg" alt="Build Status"/></a>
    <a href="https://github.com/{{.Org}}/{{.Name}}/blob/main/LICENSE"><img src="https://img.shields.io/github/license/{{.Org}}/{{.Name}}" alt="License"/></a>
    <a href="https://github.com/hop-top/12-factor-ai-cli-apps"><img src="https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/{{.Org}}/{{.Name}}/main/.12fc.json" alt="12-factor AI-CLI"/></a>
</p>

## Pick your path

| You want to… | Go to |
|--------------|-------|
| Install and run the CLI | [Get started](#30-second-quickstart) |
| Drive {{.Name}} from an agent | [Agent surface](#what-name-is) |
| Call {{.Name}} over HTTP or a socket | [Serve it](#serve-it) |
| Embed {{.Name}} in your own code | [Library API](https://pkg.go.dev/{{.Module}}) |
| Look up a command or flag | `{{.Name}} --help` · `{{.Name}} spec --format json` |
| Debug a failure | [Troubleshooting](docs/troubleshooting.md) |

## 30-second quickstart

```sh
go install {{.Module}}/cmd/{{.Name}}@latest
{{.Name}} --help
{{.Name}} --version
```

Output formats:

```sh
{{.Name}} --format json
{{.Name}} --format yaml
```

## What {{.Name}} is

Four surfaces, one binary:

- **CLI** — interactive + scriptable. Stable schemas, structured errors,
  preview via `--dry-run`, queries parse via `--explain`.
- **Capability surface** — `{{.Name}} spec --format json` emits a
  machine-readable manifest of every command, its inputs, outputs, side
  effects, and idempotency. Agents drive {{.Name}} from this, not from
  `--help`.
- **Served commands** — `{{.Name}} serve` projects the same commands
  onto REST and a local Unix socket, behind the same safety policy.
  See [Serve it](#serve-it).
- **Library** — `import "{{.Module}}"` and use the same primitives from
  your own Go code.

Conformance proof: [`docs/12-factor-conformance.md`](docs/12-factor-conformance.md)
(auto-regenerated by `make conformance`).

## Serve it

Every command in `cmd/` is also a route. There is nothing to mount:

```sh
{{.Name}} serve --list        # the registered services and their state
{{.Name}} serve api           # REST on 127.0.0.1:8080
{{.Name}} serve socket        # the same commands over an owner-only Unix socket
{{.Name}} serve               # every enabled service under one lifecycle
```

`serve --list` names every registered service and, per service,
whether the supervisor has a configuration for it (CONFIGURED — from
your config file, or from `api`'s built-in default), whether a bare
`serve` would start it (ENABLED — `api` is on by default, `socket` is
off until you enable it), and whether it is accepting work right now
(READY — true only inside a running `serve`, so always false when you
ask from the shell). A fresh project prints:

```console
$ {{.Name}} serve --list
SERVICE              CONFIGURED  ENABLED  READY
api                  true        true     false
socket               false       false    false
```

```sh
curl -s http://127.0.0.1:8080/v1/commands | jq '.commands[] | {name, invocable, reason}'
curl -s 'http://127.0.0.1:8080/v1/commands/hello?arg=Ada'
```

```json
{"exit_code":0,"data":[{"name":"Ada","message":"Hello, Ada!"}]}
```

What holds on every served surface:

- **Loopback by default.** The api listens on `127.0.0.1:8080` and
  refuses, at exit 2, to serve unauthenticated anywhere else. Set
  `Auth` in `cmd/root.go` to serve beyond loopback, or opt in by name
  with `services.api.insecure_remote: true`.
- **Reads are `GET`, writes are `POST`, destructive commands are
  withheld** until you name the surface in `APIConfig.Policy` or
  `SocketConfig.Policy` — and even then the command's own `confirm`
  gate still applies.
- **Discovery tells you why.** A command that is not mounted still
  appears in `/v1/commands` with `invocable: false` and a reason:
  `unauthorized-destructive`, `interactive`, `self-hosting`,
  `management-only`.
- **Readiness is in the trace.** The `ready_reported` log line carries
  the bound address under `address=`; wait for it rather than for a
  banner.

Move the listen address into configuration rather than a flag:

```yaml
# ~/.config/{{.Name}}/config.yaml
services:
  api:
    addr: 127.0.0.1:9090
  socket:
    enabled: true
```

Add a service of your own with `cli.WithService` in `cmd/root.go`; it
appears in `serve --list` beside `api` and `socket` and starts under
the same supervisor.

<!-- kit:agents-fragment — the AGENTS.md template fragment describing the
     served surface to agents is inserted here by the kit template. -->

## Configuration

Config file: `~/.config/{{.Name}}/config.yaml` (also
`/etc/{{.Name}}/config.yaml` and the nearest `.kit/config.yaml`,
`.kit.yaml`, or `kit.yaml` walking up from the working directory).
Environment variables prefixed with `{{.NameUpper}}_` override the
files: `{{.NameUpper}}_SERVICES_API_ADDR` sets `services.api.addr`.
`-c <path>` merges one more file; `-c key=value` overrides one key.

## Project structure

- [`docs/`](docs/) — user-facing docs (install, commands, configuration,
  troubleshooting).
- [`docs/12-factor-conformance.md`](docs/12-factor-conformance.md) —
  auto-generated conformance report.
- [`.12fc.json`](.12fc.json) — machine-readable
  conformance summary (drives the badge above).
- [`CONTRIBUTING.md`](CONTRIBUTING.md) — local development.
- [`RELEASING.md`](RELEASING.md) — version cuts via release-please.

## Getting started

Toolchain is pinned in `mise.toml`. Install everything in one go:

```sh
mise install                # installs Go (and other pinned tools)
mise run install            # downloads ecosystem deps (go mod download)
cp .env.example .env        # adapter defaults (telemetry, storage, queue, log, config)
```

Optional — start telemetry services (otel-collector + jaeger):

```sh
docker compose -f .devcontainer/docker-compose.yml up -d
open http://localhost:16686 # Jaeger UI for traces
```

## Tool versions

Go toolchain, linters, and release tooling are pinned in
`mise.toml` at the project root, inside a `# >>> kit-managed >>>`
block tracked against poly-kit's central manifest. User-added tools
above the marker are preserved verbatim.

Refresh procedure (`kit init --check` / `kit init --update`) is
documented in poly-kit's
[templates/RUNBOOK-UPGRADE.md](https://github.com/hop-top/poly-kit/blob/main/templates/RUNBOOK-UPGRADE.md).

## Development

Prerequisites: `mise` (installs the pinned Go toolchain), GNU Make.

```sh
make setup    # download deps
make check    # lint + test + links
make build    # build binary
```

### Put `{{.Name}}` on your PATH

```sh
make symlink
```

`make symlink` builds the binary, walks `$PATH`, picks the first
writable user-bin dir (in priority order: `$XDG_BIN_HOME`,
`~/.local/bin`, `~/bin` on Unix; `%USERPROFILE%\bin`,
`%USERPROFILE%\.local\bin`, `%LOCALAPPDATA%\Programs` on Windows),
and links `bin/{{.Name}}` into it. On native Windows it writes a
`.cmd` shim instead of a symlink to avoid the Admin / Developer
Mode requirement.

Subsequent `make build` runs auto-deploy because the link points
at the live `./bin/{{.Name}}`.

```sh
make symlink                       # idempotent: no-op when already linked
make symlink SYMLINK_DIR=/opt/bin  # override candidate-dir search
make symlink FORCE=1               # replace a link pointing elsewhere
```

If no candidate dir is on `$PATH`, the command prints a hint
asking you to add `$HOME/.local/bin` to `$PATH` or pass
`SYMLINK_DIR=`.

## License

See [LICENSE](LICENSE).

---
Maintained by {{.Author}}.
