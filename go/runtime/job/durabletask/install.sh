#!/usr/bin/env bash
set -euo pipefail
# durabletask-go auto-installer
# Pure Go + SQLite — no external runtime required.

ENGINE="durabletask"

echo "$ENGINE: no external runtime required (pure Go + SQLite)"
