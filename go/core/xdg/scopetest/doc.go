// Package scopetest is intentionally empty; it exists only to host tests
// that wire kit/xdg through kit/scope. Lives in its own subpackage to avoid
// the import cycle that would result from xdg_test importing scope.
package scopetest
