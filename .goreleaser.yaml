version: 2
project_name: kit

builds:
  - id: spaced
    dir: examples/spaced/go
    binary: spaced
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: spaced
    builds: [spaced]
    name_template: >-
      spaced_{{.Version}}_{{.Os}}_{{.Arch}}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - '^chore:'
      - '^ci:'
      - '^docs:'
      - '^test:'

release:
  github:
    owner: hop-top
    name: kit
  draft: false
  prerelease: auto
