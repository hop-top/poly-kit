# extensions

Optional modules that extend kit-based servers beyond the core packages; each is its own Go module.

## Contents

| Path | What it is | Start here when |
|------|------------|-----------------|
| [`mcp-tasks/`](mcp-tasks/README.md) | server side of the MCP tasks extension (`io.modelcontextprotocol/tasks`) for the official MCP Go SDK: durable, pollable long-running tool calls | an MCP tool call outlives a single request |
