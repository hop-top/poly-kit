default: true
MD013: false          # line length handled by AGENTS.md rule
MD014: false          # $ before commands is intentional
MD024: false          # duplicate headings — per-language sections repeat
MD025: false          # multiple H1 — spec frontmatter title + H1 is intentional
MD031: false          # blanks around fences
MD032: false          # blanks around lists
MD033: false          # allow inline HTML
MD036: false          # emphasis as heading
MD040: false          # fenced code language
MD041: false          # first line heading not required
MD042: false          # empty links (badge placeholders)
MD060: false          # table column style
