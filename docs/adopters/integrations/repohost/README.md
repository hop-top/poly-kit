# repohost

Provider conventions for kit's repohost facade over `github.com/drone/go-scm`.

## Contents

| Path | What it is | Start here when |
|------|------------|-----------------|
| [`auth.md`](auth.md) | per-provider authentication conventions, matching what go-scm expects | you configure credentials for a provider behind `Host` / `MutableHost` |
